import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Calendar } from "@/components/ui/calendar";
import { Popover, PopoverContent, PopoverTrigger } from "@/components/ui/popover";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { toast } from "sonner";
import { AlertTriangle, ArrowDownToLine, ArrowUpFromLine, Boxes, CalendarDays, Check, ClipboardEdit, Filter, History, Loader2, PackageCheck, Plus, RefreshCw, Search, X } from "lucide-react";
import type { LucideIcon } from "lucide-react";
import { format } from "date-fns";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";
import { trpc } from "@/lib/trpc";

type MovementType = "entry" | "consumption" | "loss" | "adjustment" | "reservation" | "release_reservation";
type ProductMovementType = "product_out" | "product_adjustment";
type InputUnit = "g" | "kg" | "roll" | "unit" | "m";
const unitLabels: Record<InputUnit, string> = { g: "g", kg: "kg", roll: "rolo", unit: "un", m: "m" };
const compatibleUnits = (item?: { baseUnit: "weight" | "unit" | "length"; weightPerUnit: number | null }) => item?.baseUnit === "unit" ? ["unit" as InputUnit] : item?.baseUnit === "length" ? ["m" as InputUnit] : item?.weightPerUnit ? ["g", "kg", "roll"] as InputUnit[] : ["g", "kg"] as InputUnit[];
const types: Record<MovementType | ProductMovementType, { label: string; short: string; icon: LucideIcon; className: string; sign: string }> = {
  entry: { label: "Entrada", short: "Entrada", icon: ArrowDownToLine, className: "border-[#24D18A]/20 bg-[#24D18A]/10 text-[#5DE7AB]", sign: "+" },
  consumption: { label: "Consumo em impressão", short: "Consumo", icon: ArrowUpFromLine, className: "border-[#168BFF]/20 bg-[#168BFF]/10 text-[#55B3FF]", sign: "−" },
  loss: { label: "Perda / descarte", short: "Perda", icon: AlertTriangle, className: "border-red-400/20 bg-red-500/10 text-red-300", sign: "−" },
  adjustment: { label: "Ajuste", short: "Ajuste", icon: ClipboardEdit, className: "border-[#7C5CFF]/20 bg-[#7C5CFF]/10 text-[#B9A9FF]", sign: "±" },
  reservation: { label: "Reserva", short: "Reserva", icon: PackageCheck, className: "border-[#FFB547]/20 bg-[#FFB547]/10 text-[#FFCB70]", sign: "−" },
  release_reservation: { label: "Liberação de reserva", short: "Liberação", icon: RefreshCw, className: "border-cyan-400/20 bg-cyan-400/10 text-cyan-300", sign: "+" },
  product_out: { label: "Saída de produto", short: "Saída", icon: ArrowUpFromLine, className: "border-amber-400/20 bg-amber-400/10 text-amber-200", sign: "−" },
  product_adjustment: { label: "Ajuste de produto", short: "Ajuste", icon: ClipboardEdit, className: "border-[#7C5CFF]/20 bg-[#7C5CFF]/10 text-[#B9A9FF]", sign: "±" },
};
const typeOptions = Object.entries(types).filter(([value]) => !value.startsWith("product_")) as [MovementType, typeof types[MovementType]][];
const historyTypeLabels: Record<MovementType | ProductMovementType, string> = { ...Object.fromEntries(typeOptions.map(([value, item]) => [value, item.label])) as Record<MovementType, string>, product_out: "Saída de produto", product_adjustment: "Ajuste de produto" };
const productReasonLabels: Record<string, string> = { sale: "Venda", delivery: "Entrega", internal_use: "Uso interno", adjustment: "Ajuste", other: "Outro", manual: "Manual" };
const formatWeight = (value: unknown) => `${Number(value ?? 0).toLocaleString("pt-BR", { maximumFractionDigits: 2 })} g`;
const formatBase = (value: unknown, baseUnit: "weight" | "unit" | "length") => `${Number(value ?? 0).toLocaleString("pt-BR", { maximumFractionDigits: baseUnit === "unit" ? 0 : 3 })} ${baseUnit === "unit" ? "un" : baseUnit === "length" ? "m" : "g"}`;
const formatDate = (value: Date | string) => new Date(value).toLocaleString("pt-BR", { dateStyle: "short", timeStyle: "short" });
const friendlyDescription = (description: string | null | undefined) => description?.replace(/Produção de (\d+) unidade\(s\) de [0-9a-f-]{20,}/gi, "Produção de $1 unidade(s)") || "—";

function DateFilter({ value, onChange, placeholder }: { value: string; onChange: (value: string) => void; placeholder: string }) {
  const [open, setOpen] = useState(false);
  const selected = value ? new Date(`${value}T12:00:00`) : undefined;
  return <Popover open={open} onOpenChange={setOpen}><PopoverTrigger asChild><Button type="button" variant="outline" className="h-11 w-full justify-between border-white/[.08] bg-[#101A2A] px-3 text-left font-normal text-slate-200 hover:bg-[#152338] hover:text-white"><span className={selected ? "text-slate-200" : "text-slate-500"}>{selected ? format(selected, "dd/MM/yyyy") : placeholder}</span><CalendarDays className="h-4 w-4 text-slate-300" /></Button></PopoverTrigger><PopoverContent align="start" className="w-auto border-white/10 bg-[#101A2A] p-0 text-slate-100 shadow-2xl"><Calendar mode="single" selected={selected} onSelect={date => { onChange(date ? format(date, "yyyy-MM-dd") : ""); setOpen(false); }} className="!bg-[#101A2A] text-slate-100" /></PopoverContent></Popover>;
}

export default function Movements() {
  const [, setLocation] = useLocation();
  const list = trpc.movements.list.useQuery();
  const filaments = trpc.filaments.list.useQuery();
  const utils = trpc.useUtils();
  const [query, setQuery] = useState("");
  const [typeFilter, setTypeFilter] = useState("all");
  const [filamentFilter, setFilamentFilter] = useState("all");
  const [from, setFrom] = useState("");
  const [to, setTo] = useState("");
  const [open, setOpen] = useState(false);
  const [form, setForm] = useState({ filamentId: "", type: "entry" as MovementType, inputUnit: "g" as InputUnit, quantity: "", newWeight: "", description: "" });
  const create = trpc.movements.create.useMutation({
    onSuccess: () => {
      toast.success("Movimentação registrada", { description: "Saldo e histórico atualizados com segurança." });
      void utils.movements.list.invalidate();
      void utils.filaments.list.invalidate();
      void utils.filaments.summary.invalidate();
      setOpen(false);
      setForm({ filamentId: "", type: "entry", inputUnit: "g", quantity: "", newWeight: "", description: "" });
    },
    onError: error => toast.error("Não foi possível registrar a movimentação", { description: error.message }),
  });

  const selected = filaments.data?.find(item => item.id === Number(form.filamentId));
  const quantity = Number(form.quantity || 0);
  const newWeight = Number(form.newWeight || 0);
  const inputUnits = compatibleUnits(selected);
  const selectedUnit = form.inputUnit;
  const convertedQuantity = selected ? (form.type === "adjustment" ? newWeight : selected.baseUnit === "unit" || selected.baseUnit === "length" ? quantity : selectedUnit === "kg" ? quantity * 1000 : selectedUnit === "roll" ? quantity * Number(selected.weightPerUnit) : quantity) : 0;
  const preview = selected ? (form.type === "adjustment" ? newWeight : Number(selected.currentWeight) + (types[form.type].sign === "+" ? convertedQuantity : -convertedQuantity)) : 0;
  const validPreview = Boolean(selected && preview >= 0 && (form.type === "adjustment" ? form.newWeight !== "" && (selected.baseUnit === "weight" || Number.isInteger(newWeight)) : quantity > 0 && (selected.baseUnit === "weight" || Number.isInteger(quantity)) && inputUnits.includes(selectedUnit)));

  const filtered = useMemo(() => (list.data ?? []).filter(item => {
    const haystack = `${item.entityName} ${item.filamentMaterial ?? ""} ${item.filamentBrand ?? ""} ${item.filamentColor ?? ""} ${item.description ?? ""} ${item.reason ? productReasonLabels[item.reason] ?? item.reason : ""}`.toLowerCase();
    const date = new Date(item.createdAt).getTime();
    const start = from ? new Date(`${from}T00:00:00`).getTime() : -Infinity;
    const end = to ? new Date(`${to}T23:59:59`).getTime() : Infinity;
    return haystack.includes(query.toLowerCase()) && (typeFilter === "all" || item.type === typeFilter) && (filamentFilter === "all" || (item.entityType === "filament" && String(item.filamentId) === filamentFilter)) && date >= start && date <= end;
  }), [list.data, query, typeFilter, filamentFilter, from, to]);

  function submit(event: React.FormEvent) {
    event.preventDefault();
    if (!selected || !validPreview) { toast.error("Revise os dados da movimentação", { description: preview < 0 ? "O saldo não pode ficar negativo." : "Informe uma quantidade válida." }); return; }
    create.mutate({ filamentId: selected.id, type: form.type, inputUnit: form.type === "adjustment" ? selected.baseUnit === "unit" ? "unit" : selected.baseUnit === "length" ? "m" : "g" : form.inputUnit, inputQuantity: form.type === "adjustment" ? 0 : quantity, adjustmentWeight: form.type === "adjustment" ? newWeight : undefined, description: form.description || null });
  }

  return <div className="mx-auto max-w-[1500px] space-y-7">
    <header className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><button onClick={() => setLocation("/estoque")} className="mb-4 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.2em] text-slate-500 hover:text-[#55B3FF]"><span>Estoque</span><span>/</span><span className="text-[#55B3FF]">Movimentações</span></button><h1 className="font-display text-3xl font-semibold tracking-[-.04em] text-white sm:text-4xl">Movimentações</h1><p className="mt-2 text-sm text-slate-500">Acompanhe entradas e saídas de filamentos e produtos prontos em um único histórico.</p></div><Button onClick={() => setOpen(true)} className="h-11 rounded-xl bg-[#168BFF] px-5 font-semibold text-white hover:bg-[#0d78df]"><Plus className="mr-2 h-4 w-4" />Nova movimentação</Button></header>
    <Card className="border-white/[.07] bg-[#0B1220] shadow-none"><CardContent className="p-4"><div className="grid gap-3 lg:grid-cols-2 xl:grid-cols-3 2xl:grid-cols-[minmax(220px,1fr)_180px_180px_140px_140px]"><div className="relative"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-600" /><Input value={query} onChange={event => setQuery(event.target.value)} placeholder="Buscar por filamento, cor ou descrição..." className="h-11 border-white/[.08] bg-white/[.03] pl-10 text-white placeholder:text-slate-600 focus-visible:ring-[#168BFF]" /></div><Select value={typeFilter} onValueChange={setTypeFilter}><SelectTrigger className="h-11 w-full border-white/[.08] bg-[#101A2A] text-slate-200 focus-visible:border-[#168BFF]"><SelectValue placeholder="Todos os tipos" /></SelectTrigger><SelectContent className="border-white/10 bg-[#101A2A] text-slate-100"> <SelectItem value="all">Todos os tipos</SelectItem>{typeOptions.map(([value, item]) => <SelectItem key={value} value={value}>{item.label}</SelectItem>)}<SelectItem value="product_out">Saída de produto</SelectItem><SelectItem value="product_adjustment">Ajuste de produto</SelectItem></SelectContent></Select><Select value={filamentFilter} onValueChange={setFilamentFilter}><SelectTrigger className="h-11 w-full border-white/[.08] bg-[#101A2A] text-slate-200 focus-visible:border-[#168BFF]"><SelectValue placeholder="Todos os filamentos" /></SelectTrigger><SelectContent className="border-white/10 bg-[#101A2A] text-slate-100"><SelectItem value="all">Todos os filamentos</SelectItem>{(filaments.data ?? []).map(item => <SelectItem key={item.id} value={String(item.id)}>{item.material} · {item.color}</SelectItem>)}</SelectContent></Select><DateFilter value={from} onChange={setFrom} placeholder="Data inicial" /><DateFilter value={to} onChange={setTo} placeholder="Data final" /></div>{(query || typeFilter !== "all" || filamentFilter !== "all" || from || to) && <div className="mt-3 flex items-center gap-2 text-xs text-slate-500"><Filter className="h-3 w-3" /><span>{filtered.length} resultado{filtered.length === 1 ? "" : "s"}</span><button onClick={() => { setQuery(""); setTypeFilter("all"); setFilamentFilter("all"); setFrom(""); setTo(""); }} className="flex items-center gap-1 text-[#55B3FF] hover:text-white"><X className="h-3 w-3" />Limpar filtros</button></div>}</CardContent></Card>
    <Card className="border-white/[.07] bg-[#0B1220] shadow-none"><CardContent className="p-0"><div className="hidden grid-cols-[150px_minmax(220px,1.4fr)_170px_110px_110px_minmax(170px,1fr)_140px] gap-4 border-b border-white/[.07] px-5 py-4 font-mono text-[10px] uppercase tracking-[.15em] text-slate-600 lg:grid"><span>Data e hora</span><span>Item</span><span>Tipo</span><span>Quantidade</span><span>Saldo</span><span>Descrição</span><span>Usuário</span></div>{list.isLoading ? <div className="p-14 text-center text-sm text-slate-500"><Loader2 className="mx-auto mb-3 h-5 w-5 animate-spin text-[#168BFF]" />Carregando histórico...</div> : list.isError ? <div className="p-14 text-center"><AlertTriangle className="mx-auto h-7 w-7 text-red-300" /><h3 className="mt-4 font-display font-semibold text-white">Falha ao carregar movimentações</h3><Button variant="outline" onClick={() => { void list.refetch(); }} className="mt-5 border-white/10 bg-transparent text-slate-300">Tentar novamente</Button></div> : filtered.length === 0 ? <div className="p-14 text-center"><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl border border-dashed border-[#168BFF]/30 bg-[#168BFF]/5 text-[#55B3FF]"><History className="h-6 w-6" /></div><h3 className="mt-5 font-display text-lg font-semibold text-white">Nenhuma movimentação encontrada</h3><p className="mx-auto mt-2 max-w-md text-sm leading-6 text-slate-500">Registre a primeira entrada, saída ou conferência para criar o histórico da operação.</p><Button onClick={() => setOpen(true)} className="mt-6 rounded-xl bg-[#168BFF] text-white hover:bg-[#0d78df]"><Plus className="mr-2 h-4 w-4" />Registrar movimentação</Button></div> : <div className="divide-y divide-white/[.05]">{filtered.map(item => { const meta = types[item.type as keyof typeof types]; const Icon = meta.icon; const relatedFilament = filaments.data?.find(f => f.id === item.filamentId); const isLow = relatedFilament ? relatedFilament.currentWeight <= relatedFilament.minimumWeight : false; return <div key={item.id} className="grid gap-3 px-5 py-5 transition-colors hover:bg-white/[.02] lg:grid-cols-[150px_minmax(220px,1.4fr)_170px_110px_110px_minmax(170px,1fr)_140px] lg:items-center"><div className="font-mono text-xs text-slate-500">{formatDate(item.createdAt)}</div><div className="flex items-center gap-3"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-lg border border-white/10 bg-white/[.03] text-[#55B3FF]"><Boxes className="h-4 w-4" /></div><div className="min-w-0"><div className="truncate font-medium text-white">{item.filamentBrand} · {item.filamentMaterial}</div><div className="mt-1 truncate font-mono text-[10px] uppercase tracking-wider text-slate-500">{item.filamentColor}</div></div></div><div><Badge className={`${meta.className} gap-1.5`}><Icon className="h-3 w-3" />{meta.short}</Badge>{item.type === "loss" && <div className="mt-1 font-mono text-[9px] uppercase text-red-300/60">atenção</div>}</div><div className={item.type === "loss" ? "font-mono text-sm text-red-300" : "font-mono text-sm text-slate-200"}>{meta.sign}{Number(item.inputQuantity ?? item.quantityGrams).toLocaleString("pt-BR", { maximumFractionDigits: item.inputUnit === "unit" ? 0 : 3 })} {unitLabels[item.inputUnit] ?? "g"}<div className="mt-1 text-[10px] text-slate-600">({formatBase(item.quantityBase ?? item.quantityGrams, item.baseUnit)})</div></div><div><div className="font-mono text-sm text-white">{formatBase(item.resultingBalance ?? item.resultingWeightGrams, item.baseUnit)}</div><div className={`mt-1 font-mono text-[9px] uppercase ${isLow ? "text-[#FFCB70]" : "text-slate-600"}`}>{isLow ? "saldo baixo" : `antes ${formatBase(item.previousBalance ?? item.previousWeightGrams, item.baseUnit)}`}</div></div><div className="truncate text-sm text-slate-500">{item.entityType === "product" ? `${productReasonLabels[item.reason ?? ""] ?? "Operação"}${item.description ? ` · ${item.description}` : ""}` : friendlyDescription(item.description)}</div><div className="truncate text-xs text-slate-500">{item.userName || item.userEmail || "Gestor"}</div></div>; })}</div>}</CardContent></Card>
    <Dialog open={open} onOpenChange={setOpen}><DialogContent className="max-h-[90vh] overflow-y-auto border-white/10 bg-[#0D1727] text-white sm:max-w-xl"><DialogHeader><DialogTitle className="font-display text-xl">Nova movimentação</DialogTitle><DialogDescription className="text-slate-500">Atualize o saldo do rolo e registre o motivo da operação.</DialogDescription></DialogHeader><form onSubmit={submit} className="space-y-5 pt-2"><div className="space-y-2"><Label>Filamento</Label><Select value={form.filamentId} onValueChange={value => { const item = filaments.data?.find(entry => entry.id === Number(value)); const nextUnits = compatibleUnits(item); setForm(current => ({ ...current, filamentId: value, inputUnit: nextUnits[0] ?? "g" })); }}><SelectTrigger className="h-10 w-full border-white/10 bg-[#101A2A] text-white focus-visible:border-[#168BFF]"><SelectValue placeholder="Selecione o filamento" /></SelectTrigger><SelectContent className="border-white/10 bg-[#101A2A] text-slate-100">{(filaments.data ?? []).map(item => <SelectItem key={item.id} value={String(item.id)}>{item.brand} · {item.material} · {item.color} — {formatBase(item.currentWeight, item.baseUnit)}</SelectItem>)}</SelectContent></Select></div><div className="grid gap-4 sm:grid-cols-3"><div className="space-y-2"><Label>Tipo</Label><Select value={form.type} onValueChange={value => setForm(current => ({ ...current, type: value as MovementType }))}><SelectTrigger className="h-10 w-full border-white/10 bg-[#101A2A] text-white focus-visible:border-[#168BFF]"><SelectValue placeholder="Selecione o tipo" /></SelectTrigger><SelectContent className="border-white/10 bg-[#101A2A] text-slate-100">{typeOptions.map(([value, item]) => <SelectItem key={value} value={value}>{item.label}</SelectItem>)}</SelectContent></Select></div>{form.type !== "adjustment" && <div className="space-y-2"><Label>Unidade da movimentação</Label><Select value={form.inputUnit} onValueChange={value => setForm(current => ({ ...current, inputUnit: value as InputUnit }))}><SelectTrigger className="h-10 w-full border-white/10 bg-[#101A2A] text-white focus-visible:border-[#168BFF]"><SelectValue placeholder="Selecione a unidade" /></SelectTrigger><SelectContent className="border-white/10 bg-[#101A2A] text-slate-100">{inputUnits.map(unit => <SelectItem key={unit} value={unit}>{unitLabels[unit]}</SelectItem>)}</SelectContent></Select></div>}<div className="space-y-2"><Label>{form.type === "adjustment" ? selected?.baseUnit === "unit" ? "Novo saldo real (un)" : "Novo saldo real (g)" : `Quantidade (${unitLabels[form.inputUnit]})`}</Label><Input required min="0" step={selected?.baseUnit === "unit" || form.inputUnit === "roll" ? "1" : "0.01"} type="number" value={form.type === "adjustment" ? form.newWeight : form.quantity} onChange={event => setForm(current => ({ ...current, [form.type === "adjustment" ? "newWeight" : "quantity"]: event.target.value }))} placeholder="0" className="border-white/10 bg-white/[.03] text-white" /></div></div>{selected && <div className={`rounded-xl border p-4 ${preview < 0 ? "border-red-400/30 bg-red-500/[.07]" : "border-[#168BFF]/20 bg-[#168BFF]/[.06]"}`}><div className="flex items-center justify-between gap-4"><div><div className="font-mono text-[10px] uppercase tracking-[.18em] text-slate-500">Prévia do saldo</div><div className="mt-2 text-sm text-slate-400">{formatBase(selected.currentWeight, selected.baseUnit)} <span className="px-2 text-slate-600">→</span><strong className={preview < 0 ? "text-red-300" : "text-white"}>{formatBase(preview, selected.baseUnit)}</strong>{form.type !== "adjustment" && <span className="mt-2 block text-xs text-slate-500">Equivale a {formatBase(convertedQuantity, selected.baseUnit)} no estoque.</span>}</div></div>{preview < 0 ? <AlertTriangle className="h-5 w-5 text-red-300" /> : <Check className="h-5 w-5 text-[#5DE7AB]" />}</div>{preview < 0 && <p className="mt-3 text-xs text-red-300">O saldo resultante não pode ser negativo.</p>}</div>}<div className="space-y-2"><Label>Descrição / observação <span className="text-slate-600">(opcional)</span></Label><textarea value={form.description} onChange={event => setForm(current => ({ ...current, description: event.target.value }))} placeholder="Ex.: produção da peça X, embalagem danificada..." rows={3} className="w-full resize-none rounded-md border border-white/10 bg-white/[.03] px-3 py-2 text-sm text-white outline-none placeholder:text-slate-600 focus:border-[#168BFF]" /></div><DialogFooter><Button type="button" variant="ghost" onClick={() => setOpen(false)} className="text-slate-400 hover:bg-white/[.04] hover:text-white">Cancelar</Button><Button type="submit" disabled={create.isPending || !validPreview} className="bg-[#168BFF] text-white hover:bg-[#0d78df]">{create.isPending ? <Loader2 className="mr-2 h-4 w-4 animate-spin" /> : <Check className="mr-2 h-4 w-4" />}Confirmar movimentação</Button></DialogFooter></form></DialogContent></Dialog>
  </div>;
}
